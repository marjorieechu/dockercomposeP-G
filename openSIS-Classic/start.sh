# #!/bin/bash

# service apache2 start

# #keep the container running
# tail -f /dev/null



# #!/bin/bash
# set -e  # Exit on errors

# # Start Apache in the foreground
# service apache2 start

# # Start the Grafana dashboard manager in the background
# python3 /opt/grafana_dashboard_manager.py &

# # Keep Apache and Python script running in the foreground
# exec apachectl -D FOREGROUND
