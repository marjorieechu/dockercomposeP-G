docker-compose up -d    #runs the container

docker-compose logs -f  #checks logs

docker-compose down     #stops containers without loosing data

docker-compose down -v  #deletes everytthing including data

docker-compose ls

docker stop container

one more change needed
